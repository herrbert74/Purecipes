# Purecipes Agent Guidance

`AGENTS.md` is the entry point for repository-wide agent guidance.

Use the files under this directory as extensions of `AGENTS.md`:
- `instructions/` for durable repo rules and architectural constraints.
- `skills/` for task-oriented guidance that can be loaded selectively when the work matches the skill.

Keep these files tool-agnostic and repository-specific.
